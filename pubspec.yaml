name: bolide
description: "Bolide - Your automotive marketplace"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase
  supabase_flutter: ^2.8.0
  
  # Google Sign In
  google_sign_in: ^6.2.2
  
  # UI Components
  flutter_svg: ^2.0.10+1
  cached_network_image: ^3.4.1
  carousel_slider: ^5.0.0
  font_awesome_flutter: ^10.7.0
  photo_view: ^0.15.0
  
  # State Management
  provider: ^6.1.2
  
  # Utilities
  intl: ^0.20.2
  intl_phone_number_input: ^0.7.4
  image_picker: ^1.0.7
  shared_preferences: ^2.2.2
  url_launcher: ^6.3.1
  
  # Search & Database
  sqflite: ^2.3.0
  path: ^1.8.3
  speech_to_text: ^7.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
